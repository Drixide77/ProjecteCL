func equals(a, b)
	return a = b;
endfunc

func main()
  write "Test de Strings: ";
  read s;
  n = "potato";
  m = n;
  write m;
  if equals(s, m) then write "GLaDoS" endif;
  write "Solucio: ";
  write s;
endfunc
