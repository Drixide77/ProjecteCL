func equals(a, b)
	return a = b;
endfunc

func main()
  write "Test de Strings: ";
  read s;
  f = 1.0;
  f2 = 2.9;
  read f3;
  f4 = f * f2 / f3;
  write f4;
  write 12 % 7;
  n = "potato";
  m = n;
  write m;
  if equals(s, m) then write "GLaDoS" endif;
  write "Solucio: ";
  write s;
endfunc
